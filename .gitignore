_site
.sass-cache
.jekyll-cache
.jekyll-metadata

# Vim
*~
*.sw[p_]

# Sublime Text
*.sublime-project
*.sublime-workspace

# Ruby Gem
*.gem
.bundle
**/vendor/bundle

# Node.js and NPM
node_modules
npm-debug.log*
package-lock.json
codekit-config.json

# macOS
.DS_Store

# Jekyll generated files
.jekyll-cache
.jekyll-metadata
.sass-cache
_asset_bundler_cache
_cache
_site

# Codex app local environment file
# Keep `.codex/` available for future shared project config; ignore only the
# current autogenerated environment file unless we explicitly decide to commit it.
.codex/environments/environment.toml
